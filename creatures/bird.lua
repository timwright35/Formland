bird = {
			x = 256,
			y = 256,
			x_vel = 0,
			y_vel = 0,
			jump_vel = -768,
			speed = 256,
			flySpeed = 700,
			state = "",
			h = 32,
			w = 32,
			standing = false,
			flying = true,
		}
		
function bird:update(dt)

end

function bird:draw()

end